using System.Text.Json.Serialization;

namespace Task_3
{
    internal class Program
    {
        public static string Key { get; private set; }

        static void Main(string[] args)
        {
            Key = (i, j);
          ConsoleKeyInfo key = Console.ReadKey();
            if (Dir == 'left')

            switch ()
            {
                case ConsoleKeyInfo key = Console.ReadKey();
                        Console.WriteLine("-->");
                    break;
                case ConsoleKeyInfo key = Console.ReadKey();
                        Console.WriteLine("<--");
                    break ;

            }
        }
    }
}